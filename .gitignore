# ============================================
# STM32CubeMX & Keil MDK-ARM Project .gitignore
# ============================================

# --- Keil uVision 工程文件（包含用户布局信息）---
# 注意：.uvprojx 或 .uvproj 是核心工程文件，必须保留，不要忽略
*.uvguix.*          # 用户界面布局文件（因人而异）
*.uvoptx            # 工程选项文件（可团队共享，但常包含本地设置）

# --- 编译输出目录和文件 ---
[Oo]bj*/            # 常见的输出目录，如 Obj/、obj/
[Bb]uild/           # 构建目录
[Dd]ebug/           # Debug输出目录
[Rr]elease/         # Release输出目录
*.lst               # 链接器列表文件
*.map               # 内存映射文件
*.crf               # 交叉引用文件
*.o                 # 对象文件
*.d                 # 依赖文件
*.axf               # ARM可执行文件（主要输出）
*.elf               # 类似axf的调试文件
*.hex               # Intel Hex格式文件
*.bin               # 二进制镜像文件

# --- STM32CubeMX 生成及本地文件 ---
*.mxproject         # CubeMX项目文件
*.ioc               # CubeMX工程配置（**必须保留，这是核心配置**）
[Mm]dk-[Aa]rm/      # CubeMX生成的MDK工程目录（首次生成后建议保留，后续可忽略内部生成的文件）
*.launch            # 调试启动配置
.settings/          # IDE设置目录

# --- 调试与IDE临时文件 ---
*.htm               # 链接器生成的报告
*.sct               # 分散加载文件（链接脚本，**通常需要保留**，除非自动生成）
*.jlink            # J-Link调试脚本
*.dbgconf          # 调试配置文件
*.iex              # iSystem调试文件
*.ini              # 各种配置文件（如J-Link）
*.scvd             # SystemView调试文件

# --- 操作系统和编辑器临时文件 ---
.DS_Store           # macOS目录属性文件
Thumbs.db           # Windows缩略图缓存
*.swp               # Vim交换文件
*~                  # 许多文本编辑器的备份文件

# --- 版本控制系统元数据（安全考虑）---
.gitignore
.gitattributes